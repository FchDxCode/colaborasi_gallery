import 'package:flutter/material.dart';

class AgendaScreens extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Selamat datang'),
      ),
      body: Center(
        child: Text('Ini Profile Anda'),
      ),
    );
  }
}
